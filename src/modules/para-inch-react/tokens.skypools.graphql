query Tokens($where: TokenWhereInput) {
  tokens(where: $where) {
    edges {
      node {
        id
        network
        address
        logoUri
        decimals
        symbol
      }
    }
  }
}
